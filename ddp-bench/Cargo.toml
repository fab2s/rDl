[package]
name = "ddp-bench"
version = "0.0.1"
edition = "2024"
publish = false

[features]
default = []
cuda = ["flodl/cuda"]

[dependencies]
flodl = { path = "../flodl" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
